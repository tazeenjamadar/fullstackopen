# Exercise 0.6 – New Note in Single Page App Diagram

```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a note and clicks Save

    Note right of browser: JavaScript prevents default form submission (preventDefault)

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa (JSON)
    activate server
    Note right of server: Server saves the new note
    server-->>browser: 201 Created (success response)
    deactivate server

    Note right of browser: JavaScript updates notes array in memory
    Note right of browser: DOM re-renders without full page reload
